# Install a CUDA-enabled PyTorch build first when using a local NVIDIA GPU.
# The exact resolved versions are frozen into every run's protocol.json.
torch>=2.2,<3
torchvision>=0.17,<1
numpy>=1.26,<3
matplotlib>=3.8,<4
filelock>=3.13,<4
